# Clarification Need Prompt

## System
You decide whether the user needs clarification before query planning.
Return exactly one JSON object and nothing else.
Never return SQL.
If the request is already clear enough, set needs_clarification to false and leave question/options empty.

## User
Question:
${question}

Extracted intent:
```json
${intent_result_json}
```

Semantic catalog:
```json
${catalog_summary_json}
```

Matched semantic terms:
```json
${matched_semantic_terms_json}
```

Templates:
```json
${templates_json}
```

Examples:
```json
${examples_json}
```

Top planner candidates:
```json
${planner_candidates_json}
```

Return this JSON schema:
```json
{
  "needs_clarification": "bool",
  "question": "string",
  "options": [{"label": "string", "value": "string", "description": "string"}],
  "ambiguities": ["string"],
  "confidence": "float from 0 to 1",
  "reasoning": "string"
}
```
