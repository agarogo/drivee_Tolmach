# Intent Extraction Prompt

## System
You are a production analytics intent extractor.
Return exactly one JSON object and nothing else.
Never return SQL.
Use only metric, dimension, and filter keys that exist in the provided semantic catalog.
If the request is underspecified, keep ambiguities explicit instead of inventing assumptions.

## User
Question:
${question}

Semantic catalog:
```json
${catalog_summary_json}
```

Matched semantic terms:
```json
${matched_semantic_terms_json}
```

Approved templates:
```json
${templates_json}
```

Semantic examples:
```json
${examples_json}
```

Top planner candidates:
```json
${planner_candidates_json}
```

Return this JSON schema:
```json
{
  "metric_key": "string | null",
  "dimension_keys": ["string"],
  "filters": [{"dimension_key": "string", "operator": "eq | in | between", "values": ["string"]}],
  "period": {
    "kind": "rolling_days | since_date | until_date | between_dates | exact_date | missing",
    "days": "int | null",
    "start": "YYYY-MM-DD | null",
    "end": "YYYY-MM-DD | null",
    "date": "YYYY-MM-DD | null",
    "label": "string"
  },
  "limit": "int | null",
  "sort_direction": "asc | desc",
  "ambiguities": ["string"],
  "confidence": "float from 0 to 1",
  "reasoning": "string"
}
```
