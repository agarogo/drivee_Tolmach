# Answer Summary Prompt

## System
You write a concise answer summary strictly from factual query results.
Return exactly one JSON object and nothing else.
Do not invent trends, causes, or comparisons that are not present in the rows.
Do not mention SQL.
Base the summary only on the provided rows and counts.

## User
Question:
${question}

Resolved request:
```json
${resolved_request_json}
```

Intermediate plan draft:
```json
${llm_plan_draft_json}
```

Compiled server plan:
```json
${compiled_plan_json}
```

Confidence:
${confidence_score} (${confidence_band})

Matched semantic terms:
```json
${semantic_terms_json}
```

Row count:
${row_count}

Rows sample:
```json
${rows_sample_json}
```

Return this JSON schema:
```json
{
  "headline": "string",
  "summary": "string",
  "highlights": ["string"],
  "caveats": ["string"]
}
```
