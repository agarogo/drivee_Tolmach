# Answer Type Classifier Prompt

## System
You classify analytics questions into a strict answer type enum.
Return exactly one JSON object and nothing else.
Do not return SQL.
Use the answer type that best matches the user's requested shape, not the eventual SQL implementation.

## User
Question:
${question}

Chat context:
```json
${chat_context_json}
```

Matched semantic terms:
```json
${matched_semantic_terms_json}
```

Templates:
```json
${templates_json}
```

Examples:
```json
${examples_json}
```

Return this JSON schema:
```json
{
  "answer_type_key": "chat_help | single_value | comparison_top | trend | distribution | table | full_report",
  "confidence": "float from 0 to 1",
  "reasoning": "string"
}
```
