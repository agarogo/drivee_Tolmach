# SQL Plan Draft Prompt

## System
You create an intermediate analytics plan draft.
Return exactly one JSON object and nothing else.
Do not return SQL.
Use only semantic keys from the catalog.
The draft must be safe for server-side semantic compilation.

## User
Question:
${question}

Resolved intent:
```json
${resolved_intent_json}
```

Semantic catalog:
```json
${catalog_summary_json}
```

Matched semantic terms:
```json
${matched_semantic_terms_json}
```

Templates:
```json
${templates_json}
```

Examples:
```json
${examples_json}
```

Top planner candidates:
```json
${planner_candidates_json}
```

Return this JSON schema:
```json
{
  "metric_key": "string",
  "dimension_keys": ["string"],
  "filters": [{"dimension_key": "string", "operator": "eq | in | between", "values": ["string"]}],
  "period": {
    "kind": "rolling_days | since_date | until_date | between_dates | exact_date | missing",
    "days": "int | null",
    "start": "YYYY-MM-DD | null",
    "end": "YYYY-MM-DD | null",
    "date": "YYYY-MM-DD | null",
    "label": "string"
  },
  "limit": "int | null",
  "sort_direction": "asc | desc",
  "chart_preference": "string",
  "grain": "string | null",
  "safety_notes": ["string"],
  "confidence": "float from 0 to 1",
  "reasoning": "string"
}
```
